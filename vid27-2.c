#include <stdio.h>

int main()
{
    int arr[] = {1,2,3,4,5,6};
    printf("Value at position 3 of array is %d\n", arr[3]);
    printf("the address of the first element of array is %d\n", arr[0]);   //value printing of the given value of array
    printf("the address of the first element of array is %d\n", arr);      //address printing of the given value of array
    return 0;
}
